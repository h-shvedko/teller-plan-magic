# Docker Production Environment Variables
# Production Supabase Configuration (replace with your production values)
VITE_SUPABASE_URL=https://hxrppmdwujlfpkcfumpm.supabase.co
VITE_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# Production Environment
NODE_ENV=production

# Database Configuration
POSTGRES_PASSWORD=secure-production-password-change-this
POSTGRES_DB=postgres
POSTGRES_USER=postgres

# JWT Configuration (IMPORTANT: Change this for production!)
JWT_SECRET=super-secure-production-jwt-secret-with-at-least-32-characters-long-change-this

# External API Configuration
API_EXTERNAL_URL=https://yourdomain.com
GOTRUE_SITE_URL=https://yourdomain.com
GOTRUE_URI_ALLOW_LIST=https://yourdomain.com

# SMTP Configuration (Configure with your email provider)
SMTP_HOST=smtp.your-email-provider.com
SMTP_PORT=587
SMTP_USER=your-email@yourdomain.com
SMTP_PASS=your-email-password
ADMIN_EMAIL=admin@yourdomain.com
